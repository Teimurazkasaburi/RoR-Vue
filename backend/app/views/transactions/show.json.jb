json = {
    email: @transaction.user.email,
    amount: @transaction.amount * @transaction.duration,
    f_name: @transaction.user.f_name,
    l_name: @transaction.user.l_name,
    ref_no: @transaction.ref_no
  }